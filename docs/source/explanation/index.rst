Explanation
===========

These pages explain the ideas behind djangocms-rest — the *why* rather than the *how*.
Read them when you want a mental model of how the API behaves, so the
:doc:`../reference/index` and :doc:`../how-to/index` make sense as a whole.

.. toctree::
   :maxdepth: 1

   headless
   content-model
   preview-and-versioning
   plugin-serialization
   multi-site
   caching
